# GitHub personal access token — used by the git activity (createGithubRepo
# via octokit; see src/activities/git/). Create at
# https://github.com/settings/tokens with `repo` scope.
GITHUB_PERSONAL_ACCESS_TOKEN="<ghp-...>"
